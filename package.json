{
  "name": "alexabot-eidolon-os",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4173",
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage --test-coverage-lines=79 --test-coverage-functions=87 --test-coverage-branches=70",
    "lint": "eslint src/ server/ tests/",
    "predev:multiplayer": "npm install --no-audit --no-fund",
    "dev:multiplayer": "node server/multiplayer-server.js",
    "lint:fix": "eslint src/ server/ tests/ --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
